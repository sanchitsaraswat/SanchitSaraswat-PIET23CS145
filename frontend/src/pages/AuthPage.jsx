import { useState } from 'react';
import { Link, Navigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext.jsx';
export function AuthPage({ register = false }) { const { user, login } = useAuth(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); if (user) return <Navigate to="/" replace />; const submit = async (e) => { e.preventDefault(); setBusy(true); setError(''); try { await login(register ? '/api/auth/register' : '/api/auth/login', { email, password, ...(register ? { timezone: Intl.DateTimeFormat().resolvedOptions().timeZone } : {}) }); } catch (err) { setError(err.message); } finally { setBusy(false); } }; return <main className="grid min-h-screen bg-[radial-gradient(circle_at_15%_15%,_#d1fae5,_transparent_32rem),linear-gradient(135deg,_#f8fafc,_#f0fdf4)] lg:grid-cols-2"><section className="hidden flex-col justify-between bg-emerald-950 p-12 text-white lg:flex"><div className="flex items-center gap-2.5 text-xl font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-emerald-400 text-emerald-950">✓</span>Habitly</div><div><p className="max-w-md text-4xl font-bold leading-tight tracking-tight">Small steps, held gently.</p><p className="mt-5 max-w-sm text-lg leading-8 text-emerald-100">A calmer place to show up for the routines that matter to you.</p></div><p className="text-sm text-emerald-200">Your daily rhythm, one check-in at a time.</p></section><section className="mx-auto flex w-full max-w-md items-center p-5 sm:p-8"><form className="card w-full space-y-5 p-6 sm:p-8" onSubmit={submit}><div className="lg:hidden"><div className="flex items-center gap-2 text-lg font-bold"><span className="grid h-8 w-8 place-items-center rounded-xl bg-mint text-white">✓</span>Habitly</div></div><div><p className="eyebrow">{register ? 'Start your practice' : 'Good to see you'}</p><h1 className="mt-1 text-3xl font-bold tracking-tight">{register ? 'Build a better daily rhythm.' : 'Welcome back.'}</h1><p className="mt-2 text-sm leading-6 text-slate-600">{register ? 'Make a little room for what matters, every day.' : 'Pick up where you left off.'}</p></div><div><label htmlFor="email" className="text-sm font-semibold">Email</label><input id="email" type="email" autoComplete="email" required className="field" placeholder="you@example.com" value={email} onChange={(e) => setEmail(e.target.value)} /></div><div><label htmlFor="password" className="text-sm font-semibold">Password</label><input id="password" type="password" autoComplete={register ? 'new-password' : 'current-password'} minLength="12" required className="field" value={password} onChange={(e) => setPassword(e.target.value)} /><p className="mt-1.5 text-xs text-slate-500">At least 12 characters.</p></div>{error && <p role="alert" className="status-message border-red-200 bg-red-50 text-red-800">{error}</p>}<button className="primary w-full" disabled={busy}>{busy ? 'Please wait…' : register ? 'Create account' : 'Sign in'}</button><p className="text-center text-sm text-slate-600">{register ? 'Already have an account?' : 'New to Habitly?'} <Link className="font-bold text-mint hover:text-emerald-800" to={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Create one'}</Link></p></form></section></main>; }
