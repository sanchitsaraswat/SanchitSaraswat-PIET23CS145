import { useQuery, useQueryClient, useMutation } from '@tanstack/react-query';
import { api } from '../api/client.js';
import { HabitCard } from '../components/HabitCard.jsx';
export function DashboardPage() { const client = useQueryClient(); const query = useQuery({ queryKey: ['dashboard'], queryFn: () => api.get('/api/dashboard/today') }); const toggle = useMutation({ mutationFn: (habit) => habit.completedToday ? api.delete(`/api/habits/${habit.id}/completions/${query.data.date}`) : api.post(`/api/habits/${habit.id}/completions`, { date: query.data.date }), onSuccess: () => { client.invalidateQueries({ queryKey: ['dashboard'] }); client.invalidateQueries({ queryKey: ['habits'] }); } }); if (query.isLoading) return <p>Loading your day…</p>; if (query.isError) return <p role="alert" className="text-red-700">{query.error.message}</p>; const { date, habits, summary } = query.data; return <section><p className="text-sm font-semibold uppercase tracking-wider text-mint">Today</p><div className="mt-1 flex flex-wrap items-end justify-between gap-2"><h1 className="text-3xl font-bold">Your daily rhythm</h1><p className="text-slate-600">{summary.completed} of {summary.total} complete</p></div><div className="mt-7 space-y-3">{habits.length ? habits.map((habit) => <HabitCard key={habit.id} habit={habit} onToggle={toggle.mutate} busy={toggle.isPending}/>) : <div className="card text-center"><h2 className="font-semibold">Nothing scheduled for today</h2><p className="mt-1 text-sm text-slate-600">Add a habit, or enjoy the breathing room.</p></div>}</div></section>; }
