import { useState } from 'react';
const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
export function HabitForm({ onSubmit, onCancel, initial }) { const [name, setName] = useState(initial?.name || ''); const [description, setDescription] = useState(initial?.description || ''); const [scheduleType, setScheduleType] = useState(initial?.scheduleType || 'DAILY'); const [weekdays, setWeekdays] = useState(initial?.schedule?.map((d) => d.weekday) || [1,2,3,4,5]); const [error, setError] = useState(''); const submit = async (event) => { event.preventDefault(); if (!name.trim()) return setError('A habit name is required.'); if (scheduleType === 'CUSTOM' && !weekdays.length) return setError('Choose at least one day.'); setError(''); await onSubmit({ name, description: description || null, scheduleType, weekdays: scheduleType === 'CUSTOM' ? weekdays : [] }); }; return <form className="card space-y-4" onSubmit={submit}><div><label htmlFor="habit-name" className="font-medium">Habit name</label><input id="habit-name" className="field" value={name} maxLength="100" onChange={(e) => setName(e.target.value)} /></div><div><label htmlFor="description" className="font-medium">Description <span className="font-normal text-slate-500">optional</span></label><textarea id="description" className="field" value={description} maxLength="1000" onChange={(e) => setDescription(e.target.value)} /></div><fieldset><legend className="font-medium">Schedule</legend><select className="field" value={scheduleType} onChange={(e) => setScheduleType(e.target.value)}><option value="DAILY">Every day</option><option value="CUSTOM">Selected days</option></select>{scheduleType === 'CUSTOM' && <div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-4">{days.map((day, index) => <label key={day} className="flex items-center gap-2 text-sm"><input type="checkbox" checked={weekdays.includes(index)} onChange={() => setWeekdays((list) => list.includes(index) ? list.filter((d) => d !== index) : [...list, index])}/>{day}</label>)}</div>}</fieldset>{error && <p role="alert" className="text-sm text-red-700">{error}</p>}<div className="flex gap-3"><button className="primary" type="submit">{initial ? 'Save changes' : 'Create habit'}</button>{onCancel && <button className="secondary" type="button" onClick={onCancel}>Cancel</button>}</div></form>; }
