import { useState } from 'react';
const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
export function HabitForm({ onSubmit, onCancel, initial }) { const [name, setName] = useState(initial?.name || ''); const [description, setDescription] = useState(initial?.description || ''); const [scheduleType, setScheduleType] = useState(initial?.scheduleType || 'DAILY'); const [weekdays, setWeekdays] = useState(initial?.schedule?.map((d) => d.weekday) || [1,2,3,4,5]); const [error, setError] = useState(''); const submit = async (event) => { event.preventDefault(); if (!name.trim()) return setError('A habit name is required.'); if (scheduleType === 'CUSTOM' && !weekdays.length) return setError('Choose at least one day.'); setError(''); await onSubmit({ name, description: description || null, scheduleType, weekdays: scheduleType === 'CUSTOM' ? weekdays : [] }); }; return <form className="card space-y-5" onSubmit={submit}><div><p className="eyebrow">{initial ? 'Fine tune your routine' : 'A small promise to yourself'}</p><h2 className="mt-1 text-xl font-bold tracking-tight">{initial ? 'Habit details' : 'Create a habit'}</h2></div><div><label htmlFor="habit-name" className="text-sm font-semibold text-slate-800">Habit name</label><input id="habit-name" className="field" placeholder="e.g. Read for 20 minutes" value={name} maxLength="100" onChange={(e) => setName(e.target.value)} /></div><div><label htmlFor="description" className="text-sm font-semibold text-slate-800">Description <span className="font-normal text-slate-500">optional</span></label><textarea id="description" className="field" placeholder="A little context can make a habit easier to keep." value={description} maxLength="1000" onChange={(e) => setDescription(e.target.value)} /></div><fieldset><legend className="text-sm font-semibold text-slate-800">Schedule</legend><select aria-label="Schedule type" className="field" value={scheduleType} onChange={(e) => setScheduleType(e.target.value)}><option value="DAILY">Every day</option><option value="CUSTOM">Selected days</option></select>{scheduleType === 'CUSTOM' && <div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-4">{days.map((day, index) => <label key={day} className={`flex cursor-pointer items-center gap-2 rounded-lg border px-3 py-2 text-sm transition ${weekdays.includes(index) ? 'border-emerald-200 bg-emerald-50 text-emerald-900' : 'border-slate-200 hover:bg-slate-50'}`}><input className="h-4 w-4 rounded border-slate-300 text-mint focus:ring-mint" type="checkbox" checked={weekdays.includes(index)} onChange={() => setWeekdays((list) => list.includes(index) ? list.filter((d) => d !== index) : [...list, index])}/>{day}</label>)}</div>}</fieldset>{error && <p role="alert" className="status-message border-red-200 bg-red-50 text-red-800">{error}</p>}<div className="flex flex-wrap gap-3"><button className="primary" type="submit">{initial ? 'Save changes' : 'Create habit'}</button>{onCancel && <button className="secondary" type="button" onClick={onCancel}>Cancel</button>}</div></form>; }
