export function HabitCard({ habit, onToggle, onArchive, onEdit, busy }) { return <article className="card flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="text-lg font-semibold">{habit.name}</h3>{habit.description && <p className="mt-1 text-sm text-slate-600">{habit.description}</p>}<p className="mt-3 text-sm text-slate-500">{habit.scheduleType === 'DAILY' ? 'Every day' : habit.schedule.map((d) => ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'][d.weekday]).join(', ')} · <span aria-label="Current streak">🔥 {habit.currentStreak}</span> current · <span aria-label="Best streak">🏆 {habit.bestStreak}</span> best</p></div><div className="flex flex-wrap gap-2"><button className={habit.completedToday ? 'secondary' : 'primary'} disabled={busy} onClick={() => onToggle(habit)}>{habit.completedToday ? 'Undo today' : 'Complete today'}</button>{onEdit && <button className="secondary" onClick={() => onEdit(habit)}>Edit</button>}{onArchive && <button className="secondary" onClick={() => onArchive(habit)}>Archive</button>}</div></article>; }
