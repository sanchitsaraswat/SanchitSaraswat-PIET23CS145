export function HabitCard({ habit, onToggle, onArchive, onEdit, busy }) { const schedule = habit.scheduleType === 'DAILY' ? 'Every day' : habit.schedule.map((d) => ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'][d.weekday]).join(', '); return <article className={`card group relative overflow-hidden transition duration-200 hover:-translate-y-0.5 hover:shadow-md ${habit.completedToday ? 'border-emerald-200 bg-emerald-50/35' : ''}`}>{habit.completedToday && <span className="absolute right-0 top-0 h-16 w-16 -translate-y-8 translate-x-8 rotate-45 bg-emerald-100" aria-hidden="true" />}<div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><div className="flex items-center gap-2"><h3 className="truncate text-lg font-bold tracking-tight text-slate-900">{habit.name}</h3>{habit.completedToday && <span className="rounded-full bg-emerald-100 px-2 py-0.5 text-xs font-bold text-emerald-800">Done</span>}</div>{habit.description && <p className="mt-1 text-sm leading-6 text-slate-600">{habit.description}</p>}<div className="mt-3 flex flex-wrap items-center gap-x-3 gap-y-1 text-xs font-medium text-slate-500"><span>{schedule}</span><span className="text-slate-300">•</span><span aria-label="Current streak">🔥 {habit.currentStreak} current</span><span aria-label="Best streak">🏆 {habit.bestStreak} best</span></div></div><div className="flex flex-wrap gap-2 sm:justify-end"><button className={habit.completedToday ? 'secondary' : 'primary'} disabled={busy} onClick={() => onToggle(habit)}>{habit.completedToday ? 'Undo today' : 'Complete today'}</button>{onEdit && <button className="secondary" onClick={() => onEdit(habit)}>Edit</button>}{onArchive && <button className="secondary" onClick={() => onArchive(habit)}>Archive</button>}</div></div></article>; }
