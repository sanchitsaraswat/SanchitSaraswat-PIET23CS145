import { NavLink, Outlet } from 'react-router-dom';
import { useAuth } from '../context/AuthContext.jsx';
export function Layout() { const { user, logout } = useAuth(); return <div className="min-h-screen"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-5xl items-center justify-between px-4 py-4"><NavLink to="/" className="text-xl font-bold tracking-tight text-mint">Habitly</NavLink><nav className="flex items-center gap-4 text-sm font-medium"><NavLink to="/" end>Today</NavLink><NavLink to="/habits">Habits</NavLink><NavLink to="/archived">Archived</NavLink><span className="hidden text-slate-500 sm:inline">{user?.email}</span><button className="text-slate-600 hover:text-slate-900" onClick={logout}>Sign out</button></nav></div></header><main className="mx-auto max-w-5xl px-4 py-8"><Outlet /></main></div>; }
