import { NavLink, Outlet } from 'react-router-dom';
import { useAuth } from '../context/AuthContext.jsx';
const navClass = ({ isActive }) => `rounded-lg px-3 py-2 text-sm font-semibold transition ${isActive ? 'bg-emerald-50 text-mint' : 'text-slate-600 hover:bg-slate-100 hover:text-slate-950'}`;
export function Layout() { const { user, logout } = useAuth(); return <div className="min-h-screen bg-[radial-gradient(circle_at_top_left,_rgba(209,250,229,0.45),_transparent_28rem)]"><header className="sticky top-0 z-10 border-b border-slate-200/80 bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-x-4 gap-y-2 px-4 py-3 sm:px-6"><NavLink to="/" className="flex items-center gap-2.5 text-lg font-bold tracking-tight text-ink"><span className="grid h-8 w-8 place-items-center rounded-xl bg-mint text-sm text-white shadow-sm">✓</span>Habitly</NavLink><nav aria-label="Main navigation" className="order-3 -mx-1 flex w-full items-center gap-1 overflow-x-auto pb-0.5 sm:order-2 sm:w-auto"><NavLink to="/" end className={navClass}>Today</NavLink><NavLink to="/habits" className={navClass}>Habits</NavLink><NavLink to="/archived" className={navClass}>Archived</NavLink></nav><div className="order-2 flex items-center gap-2 sm:order-3"><span className="hidden max-w-44 truncate text-sm text-slate-500 md:inline" title={user?.email}>{user?.email}</span><button className="rounded-lg px-2.5 py-2 text-sm font-semibold text-slate-600 transition hover:bg-slate-100 hover:text-slate-950" onClick={logout}>Sign out</button></div></div></header><main className="mx-auto max-w-6xl px-4 py-7 sm:px-6 sm:py-10"><Outlet /></main></div>; }
